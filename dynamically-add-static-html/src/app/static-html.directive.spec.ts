import { StaticHtmlDirective } from './static-html.directive';

describe('StaticHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new StaticHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
